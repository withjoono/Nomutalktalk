// ChunkingService placeholder - minimal implementation
class ChunkingService {
    constructor() { }
    chunk(text, options = {}) {
        return [text];
    }
}

module.exports = ChunkingService;
